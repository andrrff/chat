<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- Set the ROOM_ID variable to the roomId that is passed in by server.js -->
    <script>
        const ROOM_ID = "<%= roomId %>"
    </script>
    <!-- CDN in the peerJS and socket.io libraries -->
    <script defer src="https://unpkg.com/peerjs@1.3.1/dist/peerjs.min.js"></script>
    <script src = "/socket.io/socket.io.js" defer></script>
    <!-- Load in script.js, our client-side script -->
    <script src = "/controllers/webrtc.js" defer></script>
    <title>Call video</title>
    <!-- Make a grid of 300x300 videos  -->
    <style>
        body{
            background: #252731;
        }
        #video-grid {
            display: inline-grid;
            float: right;
            width: auto;
            margin: 10px;
            grid-template-columns: repeat(auto-fill, 200px);
            grid-gap: 20px;
            grid-auto-rows: 200px;
        }
        #principal
        {
            display: inline-flex;
            margin: 10px;
            grid-template-columns: 100%;
            grid-gap: 20px;
            grid-auto-rows: 500px;
        }
        #video-main
        {
            margin: 10px;
            float: right;
            width: auto;
        }
        @media only screen and (max-width: 900px) {
            #principal {
                display: inline-table;
                margin: 10px;
                grid-template-columns: 100%;
                grid-gap: 20px;
                grid-auto-rows: 500px;
            }
            #video-grid
            {
                display: flex;
                align-items: center;
                justify-content: flex-start;
                width: 100%;
                overflow: auto;
                scroll-behavior: smooth;
            }
            #video-main
            {
                margin: 10px;
                height: 500px;
                float: right;
                width: auto;
            }
        }
        video {
            width: 100%;
            height: 100%;
            border-radius: 10px;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <!-- Make our video grid -->
    <h1 style="text-align: center;
    font-family: sans-serif;
    color: antiquewhite;">Room meet</h1>
    <div id="principal">
        <div id="video-main">
            <video controls preload="metadata">
                <source src="https://5.orezraey.workers.dev/0:/Animes/11eyes/%5BInfinite%5D%2011eyes%20-%2001%20%5B720p%5D.mp4" type="video/mp4"/>
                Video not supported.
            </video>
        </div>
        <div id="video-grid"></div>
    </div>
</body>
</html>
